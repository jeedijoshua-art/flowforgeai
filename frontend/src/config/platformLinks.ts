// Centralized platform links configuration
export const PLATFORM_LINKS = {
  // Internal route for NodeCraft AI workflow studio
  nodecraft: '/workflow-studio',
  // Placeholder for FlowForge main site (if needed)
  flowforge: 'https://flowforge.vercel.app',
  // Landing page URL (could be root)
  landing: '/',
};
